from firecrawl import Firecrawl
from config import FIRECRAWL_API_KEY, GITHUB_TOKEN

def process_job_description(job_description_url):
    """
    To get info for the job description into markdown files.
    """
    try:
        pass
    except:
        pass