from firecrawl import Firecrawl
from config import FIRECRAWL_API_KEY, GITHUB_TOKEN


def process_github_profile(github_url):
    """
    To get info for the githut url into markdown files.
    """
    try:
        pass
    except:
        pass